<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>{{ titre }}</title>
  <style>
    body {
      font-family: "Helvetica", Arial, sans-serif;
      max-width: 800px;
      margin: auto;
      background-color: #fafafa;
      color: #222;
      line-height: 1.6;
      padding: 20px;
    }

    h1 {
      color: #0d6efd;
      border-bottom: 2px solid #0d6efd;
      padding-bottom: 0.2em;
    }

    h2 {
      color: #333;
      margin-top: 1.5em;
    }

    ul, ol {
      margin-left: 1.5em;
    }

    .section {
      margin-bottom: 2em;
      background: #fff;
      padding: 1em;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
    }

    footer {
      margin-top: 2em;
      font-size: 0.9em;
      color: #666;
      border-top: 1px solid #ddd;
      padding-top: 1em;
    }

    /* Style impression PDF */
    @media print {
      body { background: white; color: black; }
      a { color: black; text-decoration: none; }
      .section { box-shadow: none; border: 1px solid #ccc; }
    }
  </style>
</head>
<body>

  <header>
    <h1>{{ titre }}</h1>
    <p>{{ intro }}</p>
  </header>

  <div class="section">
    <h2>🧰 Matériaux</h2>
    {% if materiaux %}
      <ul>
        {% for m in materiaux %}<li>{{ m }}</li>{% endfor %}
      </ul>
    {% else %}
      <p><em>Non spécifié</em></p>
    {% endif %}
  </div>

  <div class="section">
    <h2>🔧 Outils</h2>
    {% if outils %}
      <ul>
        {% for o in outils %}<li>{{ o }}</li>{% endfor %}
      </ul>
    {% else %}
      <p><em>Non spécifié</em></p>
    {% endif %}
  </div>

  <div class="section">
    <h2>🪜 Étapes</h2>
    {% if etapes %}
      <ol>
        {% for e in etapes %}<li>{{ e }}</li>{% endfor %}
      </ol>
    {% else %}
      <p><em>Pas encore d’étapes définies</em></p>
    {% endif %}
  </div>

  {% if remarques %}
  <footer>
    <strong>💡 Remarques :</strong> {{ remarques }}
  </footer>
  {% endif %}

</body>
</html>
